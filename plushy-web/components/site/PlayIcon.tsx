'use client'

import { CaretRightFilled } from '@ant-design/icons'

function PlayIcon({ className }: { className?: string }) {
  return <CaretRightFilled className={className} aria-hidden="true" />
}

export default PlayIcon
